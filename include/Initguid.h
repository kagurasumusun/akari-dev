/*
 * Initguid.h -- the header spelling the official CE pages print for
 * the GUID-definition header.
 *
 * Copyright (c) 2026 Akari API contributors
 * SPDX-License-Identifier: MIT
 *
 * 14 official pages name Initguid.h in their Header row. Every
 * declaration they attribute to it is already carried by this tree (a
 * few are recorded there as held, with the reason); each is listed
 * below with its page id and the header that holds it. Nothing is
 * re-declared here: a second copy of a typedef, struct or prototype is
 * a second thing to keep in step with the pages, and the pages are the
 * only evidence this tree accepts.
 *
 * A consumer that writes `#include <Initguid.h>` -- the spelling the
 * documentation uses -- therefore gets exactly the declarations
 * the documentation attributes to it.
 *
 *   IAMMediaContent Interface (ms908721) -- Dshow.h
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_AuthorName (aa451759) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_BaseURL (aa451760) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_Copyright (aa451761) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_Description (aa451762) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_LogoIconURL (aa451763) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_LogoURL (aa451764) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_MoreInfoBannerImage (aa451765) -- Dshow.h
 *   (declared as `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_MoreInfoBannerURL (aa451766) -- Dshow.h
 *   (declared as `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_MoreInfoText (aa451767) -- Dshow.h (declared
 *   as `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_MoreInfoURL (ms908698) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_Rating (ms908703) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_Title (ms908713) -- Dshow.h (declared as
 *   `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 *   IAMMediaContent::get_WatermarkURL (ms908716) -- Dshow.h (declared
 *   as `IAMMediaContent`)
 *       Windows CE 2.12 and later. Version 2.12 requires DXPAK 1.0 or
 *       later.
 */

#ifndef AKARI_INITGUID_H_
#define AKARI_INITGUID_H_

#include "Dshow.h"

#endif /* AKARI_INITGUID_H_ */
