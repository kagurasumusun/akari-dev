/*
 * Wceemul.h -- the header spelling the official CE pages print for
 * the IME candidate and composition structures.
 *
 * Copyright (c) 2026 Akari API contributors
 * SPDX-License-Identifier: MIT
 *
 * 3 official pages name Wceemul.h in their Header row. Every
 * declaration they attribute to it is already carried by this tree (a
 * few are recorded there as held, with the reason); each is listed
 * below with its page id and the header that holds it. Nothing is
 * re-declared here: a second copy of a typedef, struct or prototype is
 * a second thing to keep in step with the pages, and the pages are the
 * only evidence this tree accepts.
 *
 * A consumer that writes `#include <Wceemul.h>` -- the spelling the
 * documentation uses -- therefore gets exactly the declarations
 * the documentation attributes to it.
 *
 *   CANDIDATEFORM (_wcesdk_win32_CANDIDATEFORM_str) -- Imm.h
 *       Windows CE OS
 *   CANDIDATELIST (_wcesdk_win32_CANDIDATELIST_str) -- Imm.h
 *       Windows CE OS
 *   COMPOSITIONFORM (_wcesdk_win32_COMPOSITIONFORM_str) -- Imm.h
 *       Windows CE OS
 */

#ifndef AKARI_WCEEMUL_H_
#define AKARI_WCEEMUL_H_

#include "Imm.h"

#endif /* AKARI_WCEEMUL_H_ */
